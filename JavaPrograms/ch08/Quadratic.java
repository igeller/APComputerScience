package ch08;// FileName:  Quadratic.java  public class Quadratic {	// Instance Variables (Do Not Modify)	private int a;	private int b;	private int c;	private double root1;	private double root2;		// Write the Default Constructor and set the instance variables	// to appropriate default values.	public Quadratic ()	{		this.a = 0;		this.b = 0;		this.c = 0;		this.root1 = 0.0;		this.root2 = 0.0;	}					// Write the Initializing Constructor and set the instance variables	// a, b, & c to the parameter values passed to this constructor.  To set	// the instance variables root1 and root2, call the private helper method	// findRoots() method of this class.  Before calling findRoots() here,	// stop and write the method further down in this file.	public Quadratic (int a, int b, int c)	{		this.a = a;		this.b = b;		this.c = c;		findRoots();					}				// ACCESSOR METHODS	// Write the accessor method that will return the value of	// the instance variable a.  The method's name is getA.	public int getA()	{		return a;	}			// Write the accessor method that will return the value of	// the instance variable b.  The method's name is getB.	public int getB()	{		return this.b;	}			// Write the accessor method that will return the value of	// the instance variable c.  The method's name is getC.	public int getC()	{		return this.c;	}					// MUTATOR OR MODIFIER METHODS	// Write the mutator method that will set the instance variable a	// to the value of the int parameter.  The method's name is setA.	public void setA (int a)	{		this.a = a;	}			// Write the mutator method that will set the instance variable b	// to the value of the int parameter.  The method's name is setB.	public void setB (int b)	{		this.b = b;	}			// Write the mutator method that will set the instance variable c	// to the value of the int parameter.  The method's name is setC.	public void setC(int c)	{		this.c = c;	}				// ADDITIONAL METHODS AVAILABLE TO THIS CLASS BELOW.		// Write the method evaluateQuadratic that returns an integer value.	// This method receives an integer parameter named x.  This method	// will calculate the value y and return it based on the general	// polynomial formula:  y = ax^2 + bx + c.  Do not use Math.pow()	// to calculate x^2, because it returns a double.  Keep it simple.	// (We are using only int values for a, b, and c so that this method	// can be used with drawing programs to find an integer y value for	// a given x value so specific points in a plane can be identified.)	public int evaluateQuadratic (int x)	{		int y = (this.a*((x*x))) + (this.b*x) + this.c;		return y;	}							// Write the private helper method findRoots that will only be called	// by the initializing constructor.  Note:  private helper methods are	// NOT meant to be called by other classes, therefore they are made private.	// This method receives no parameters and returns no value. It calculates	// the values of the two roots of the quadratic polynomial and assigns the	// values to the two instance variables root1 and root2.  Make sure you use	// the quadratic formula, and the use of parenthesis is recommended to 	// accurately calculate the roots.  Double check any roots that are	// calculated to make sure they are correct.  If they are not, review your	// code implemented here to make sure your quadratic formula is correct.  	private void findRoots()	{		root1 = (-this.b + (Math.sqrt((Math.pow(this.b,2))-(4*this.a*this.c)))) / (2*this.a);		root2 = (-this.b - (Math.sqrt((Math.pow(this.b,2))-(4*this.a*this.c)))) / (2*this.a);	}						// THE TOSTRING METHOD BELOW IS COMPLETE.  NO CODE TO WRITE.	// Construct and return a string representation of a Quadratic object	// outputting the quadratic in polynomial form along with its roots.	public String toString()	{		String str;		str = "The roots of the quadratic polynomial  "				+ a + "x^2+" + b + "x+" + c + "  are:" + "\n"				+ "Root 1: " + root1 + "\n"				+ "Root 2: " + root2 + "\n";		return str;	}} // end of Quadratic class.